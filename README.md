# User Service (gRPC, Go, PostgreSQL, Docker, Migrations)

## Запуск
- Удалить все сервисы и volume:
  ```
  docker-compose down --volumes --remove-orphans
  ```

- Запуск:
  ```
  docker-compose up --build
  ```
 